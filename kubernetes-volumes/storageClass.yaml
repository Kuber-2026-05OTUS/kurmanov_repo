apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: homework-hostpath

provisioner: k8s.io/minikube-hostpath
reclaimPolicy: Retain                # сохраняет PV и данные после удаления PVC

volumeBindingMode: Immediate
