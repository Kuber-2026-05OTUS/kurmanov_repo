apiVersion: v1
kind: Service
metadata:
  name: {{ include "homework-web.fullname" . }}
  labels:
    {{- include "homework-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homework-web.selectorLabels" . | nindent 4 }}
