apiVersion: v1
kind: Service
metadata:
  name: homework-web
  namespace: homework
  labels:
    app: homework-web

spec:
  type: ClusterIP

  selector:
    app: homework-web

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
