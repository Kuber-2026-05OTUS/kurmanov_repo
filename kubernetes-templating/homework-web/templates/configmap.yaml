apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "homework-web.fullname" . }}-config
  labels:
    {{- include "homework-web.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.configmap.data | nindent 2 }}